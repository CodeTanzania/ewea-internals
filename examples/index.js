const {
  MODEL_NAME_EVENT, // => 'Event'
  COLLECTION_NAME_EVENT, // => 'events'
  PATH_NAME_EVENT, // => 'event'
} = require('../lib');

console.log(MODEL_NAME_EVENT, COLLECTION_NAME_EVENT, PATH_NAME_EVENT);
